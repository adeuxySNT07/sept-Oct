#include<stdio.h>
int main() {
	double pi = 3.1415926;
	double p = 11340.0, d = 0;
	double v = pi * r * r * r * (4.0 / 3.0);
	double m = 0;
	m = p * v;
	d = 2.0 * r;
	scanf_s("%lf", &d);
	printf("%lf", m);
	return 0;
}